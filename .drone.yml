kind: pipeline
name: default

steps:
  - name: build
    image: golang:alpine
    workspace:
      base: /go
      path: src/Recon
    commands:
      - apk add git
      - go get -u github.com/golang/dep/cmd/dep
      - dep ensure
      - go build
      - go test